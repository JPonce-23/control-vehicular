# Se puede usar DATABASE_URL en plataformas como Render/Railway.
# DATABASE_URL=postgresql://usuario:password@host:5432/base

DB_HOST=localhost
DB_PORT=5432
DB_NAME=control_vehicular_test
DB_USER=postgres
DB_PASSWORD=CAMBIAR

SECRET_KEY=CAMBIAR_POR_UNA_CLAVE_ALEATORIA_DE_AL_MENOS_32_CARACTERES
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Orígenes separados por comas, sin diagonal final.
CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500

# Opcional. Si no se configura, recuperación de contraseña responde 503.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
